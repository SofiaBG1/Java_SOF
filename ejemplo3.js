let ocupacionCancha = true;
let nombreCancha = "Bernabeu";

let mensaje = ocupacionCancha ? `La cancha llamada ${nombreCancha} está ocupada` :
`la cancha llamada ${nombreCancha} está disponible`;
console.log(mensaje);